// @flow
// @see https://developers.google.com/apps-script/reference/maps/avoid

opaque type gas$Maps$Avoid = any;

interface gas$Maps$Enum$Avoid {
  +TOLLS: gas$Maps$Avoid;
  +HIGHWAYS: gas$Maps$Avoid;
}
